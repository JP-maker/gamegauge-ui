import { TotalScorePipe } from './total-score.pipe';

describe('TotalScorePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalScorePipe();
    expect(pipe).toBeTruthy();
  });
});
